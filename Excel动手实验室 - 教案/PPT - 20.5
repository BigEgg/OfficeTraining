PPT20.5
那么我们讲完了，整个中级的功能了，让我们回顾一下。
	首先是对时间的加减，没什么特别的，就是在结果超过24小时的时候需要设置显示格式。以及数字1，代表一天的时间。还有TIME函数来创建时间。
	接着，我们讲了平均数。主要提到了可以用SUMPRODUCT来计算加权平均数
	之后是页眉页脚，和Word里的类似，没什么不同。比如说奇偶页不同，首页不同，页码之类的。
	排序和筛选，这个Excel基本上最常用到的功能之一。
		排序有：单列，多列，自定义序列，按颜色，还有按行排序
		筛选也有：自动筛选，颜色筛选，还有高级筛选。其中高级筛选包括了：重复项、条件区域、条件区域自定义条件、复制筛选结果
	再然后就是条件格式，先我们简单地过了一下，之后讲了条件格式的高级用法，创建规则，以及规则的优先级。
	最后就是VLOOKUP函数了，查阅值，查阅表格，列引用，区域查询
